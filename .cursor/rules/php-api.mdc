---
description: PHP API 端点编写约定
globs: api/**/*.php,includes/**/*.php,cron/**/*.php,database/**/*.php
alwaysApply: false
---

# PHP API conventions

- 用 `require_once` 引入 `includes/config.php` 获取 `$pdo`
- 改认证逻辑前确认该文件用 `session_check.php` 还是自行 `session_start()`
- 公司/集团 scope：查 `transaction_scope.php`、`data_capture_scope_common.php` 等同域公共文件
- 金额运算用 `api/includes/money_decimal.php`，表字段 `DECIMAL(25,8)`
- 新端点优先 `api/api_response.php`；修旧端点时保持 `{ status, message }` 风格
- `declare(strict_types=1)` 仅用于较新模块，勿给老文件强行加
- 错误写 `error_log()`，对用户返回 JSON，勿 echo HTML
