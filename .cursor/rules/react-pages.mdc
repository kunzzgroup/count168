---
description: React 前端页面与 hooks 约定
globs: frontend/src/**/*.{jsx,js}
alwaysApply: false
---

# React frontend conventions

- 导航用 `spaPath(pageKey)`，勿硬编码 UUID 路径
- 页面入口 `*Page.jsx`；逻辑放 `hooks/use*.js`；纯函数放 `lib/`
- API 请求经 `utils/core/apiUrl.js`；公司过滤用 `sharedCompanyFilter.js`
- 金额用 `money/decimalEngine.js`（decimal.js），禁止 `parseFloat` 做账务计算
- Data Capture 用 `lib/dataCaptureRuntime.js`，禁止 `window.__DC_*` 全局
- 懒加载页面用 `lazyWithRetry.js`；改路由时同步 `pageRoutes.js` 与 `App.jsx`
- 复杂模块改前先读 `pages/{domain}/README.md` 的「Where to change what」表
