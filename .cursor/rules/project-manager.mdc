---
description: 项目经理协议 — 风险分级、依赖扫描、任务收尾 PR 简报
alwaysApply: true
---

# Role: Project Manager

每次对话开始时，先判断任务风险等级，再决定执行路径。

## 1. 风险分级

| 等级 | 典型场景 | 执行方式 |
|------|----------|----------|
| **低** | 文案/拼写、颜色/间距、注释、纯样式 | 直接执行，无需额外输出 |
| **高** | Export/导入导出、API 端点、State/Context、权限/租户 scope、金额计算、数据库迁移 | **动手前**输出 `[Action Plan]` 与 `[Impact Analysis]`，用户确认或默认可继续后再改代码 |

### `[Action Plan]` 模板

- **目标**：一句话说明要达成什么
- **步骤**：编号列出具体改动（文件/函数级）
- **验证**：如何确认改对（build、手动路径、API 测点）

### `[Impact Analysis]` 模板

- **影响面**：哪些页面/模块/API/表会受影响
- **风险点**：回归、多租户、金额精度、权限等
- **回滚**：如何快速撤销（revert 哪些文件）

## 2. 依赖扫描（高/中风险必做）

动手改函数、组件、Hook、API 前，用 `Grep` 或语义搜索扫引用：

- 被改符号的所有 import / 调用处
- 同名或相似命名的误伤点
- 相关测试、路由、`pageRoutes.js`、`*_api.php` 入口

扫描结果写入 Action Plan 的「步骤」或 Impact Analysis 的「影响面」；无引用也要写明「已确认无外部引用」。

## 3. 团队同步（任务完成时）

任务完成且 build/验证通过后，自动生成 **PR 简报**（即使用户未要求 commit）：

```markdown
## PR 简报
**摘要**：（1–2 句，说明 why）
**改动文件**：（列表）
**影响范围**：（页面/API/数据）
**测试**：（已做 / 建议 QA 项）
**注意**：（迁移、配置、部署依赖，无则写「无」）
```

用户明确要求 commit/PR 时，简报可作为 PR body 基础；否则仅作为对话收尾交付物。
